# HTTP Listener Configuration
# Host for the HTTP listener (0.0.0.0 allows connections from any IP)
http.host=0.0.0.0
# Port for the HTTP listener
http.port=8081

# Application Configuration
# Unique identifier for the API in API Manager
api.id=acb-demo-s-api

# Database Configuration (placeholders for future use)
# Database host
db.host=localhost
# Database user
db.user=demo_user
